import React from 'react';

const Note = ({ id, title, description }) => {
  return (
    <div className="note" style={{ border: '1px solid #ccc', margin: '10px', padding: '10px' }}>
      <h2>{title}</h2>
      <p>{description}</p>
      <p>ID: {id}</p>
    </div>
  );
};

export default Note;
